Write a Python function `is_palindrome(s: str) -> bool` that returns whether a
string is a palindrome, ignoring case, spaces, and punctuation. Include a
pytest test file covering at least: a simple palindrome, a phrase palindrome
("A man a plan a canal Panama"), and a non-palindrome.
